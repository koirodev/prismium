/**
 * @description
 * Эта функция определяет будет ли сайт индексироваться поисковыми системами.
 * Перед деплоем на прод необходимо изменить значение на true.
 * @returns {boolean} - Всегда возвращает false.
 */

export default function isRobots() {
  return false;
}
